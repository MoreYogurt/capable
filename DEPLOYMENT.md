# 🚀 Capa-ble 部署指南

本文档提供多种部署方案，帮助你将 Capa-ble 应用发布给其他用户使用。

---

## 📋 部署前准备

### 1. 测试构建

确保项目可以正常构建：

```bash
npm install
npm run build
```

如果构建成功，会在 `dist` 目录生成静态文件。

### 2. 本地预览

测试构建后的应用：

```bash
npm run preview
```

访问 http://localhost:4173 查看效果。

---

## 🌟 方案 1: Vercel 部署（强烈推荐）

**优点**: 零配置、免费、自动部署、全球 CDN

### 步骤：

#### 1. 推送代码到 GitHub

如果还没有 GitHub 仓库：

```bash
# 在 GitHub 创建新仓库（不要初始化 README）
# 然后执行：
git remote add origin https://github.com/你的用户名/capable.git
git branch -M main
git push -u origin main
```

#### 2. 部署到 Vercel

1. 访问 [vercel.com](https://vercel.com)
2. 用 GitHub 账号登录
3. 点击 **"Add New Project"**
4. 选择 **"Import Git Repository"**
5. 找到你的 `capable` 仓库，点击 **"Import"**
6. Vercel 会自动检测到 Vite 配置
7. 直接点击 **"Deploy"**

#### 3. 完成！

- 部署完成后会得到一个 `.vercel.app` 域名
- 例如：`https://capable-xyz.vercel.app`
- 每次 `git push` 都会自动重新部署

#### 4. 绑定自定义域名（可选）

1. 在 Vercel 项目设置中点击 **"Domains"**
2. 输入你的域名（如 `capable.yourdomain.com`）
3. 按照提示在域名服务商添加 DNS 记录
4. 等待 DNS 生效即可

---

## 🎯 方案 2: Netlify 部署

**优点**: 免费、功能丰富、表单处理

### 步骤：

#### 1. 推送到 GitHub（同方案1）

#### 2. 部署到 Netlify

1. 访问 [netlify.com](https://netlify.com)
2. 用 GitHub 登录
3. 点击 **"Add new site"** → **"Import an existing project"**
4. 选择 GitHub，找到你的仓库
5. 构建设置：
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
6. 点击 **"Deploy site"**

#### 3. 完成！

- 会得到 `.netlify.app` 域名
- 同样支持自定义域名绑定

---

## 📦 方案 3: GitHub Pages（完全免费）

**优点**: 完全免费、GitHub 原生支持

### 步骤：

#### 1. 安装 gh-pages

```bash
npm install -D gh-pages
```

#### 2. 修改 package.json

添加部署脚本：

```json
{
  "scripts": {
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

#### 3. 修改 vite.config.ts

添加 base 路径（仓库名）：

```typescript
export default defineConfig({
  base: '/capable/',  // 改成你的仓库名
  // ... 其他配置
})
```

#### 4. 部署

```bash
npm run deploy
```

#### 5. 启用 GitHub Pages

1. 进入 GitHub 仓库
2. **Settings** → **Pages**
3. **Source** 选择 `gh-pages` 分支
4. 保存

访问地址：`https://你的用户名.github.io/capable/`

---

## 🖥️ 方案 4: 自己的服务器

**适用场景**: 有自己的云服务器（阿里云、腾讯云等）

### 步骤：

#### 1. 本地构建

```bash
npm run build
```

#### 2. 上传文件

将 `dist` 文件夹内容上传到服务器：

```bash
# 使用 scp
scp -r dist/* user@your-server:/var/www/capable/

# 或使用 FTP 工具（FileZilla、WinSCP 等）
```

#### 3. 配置 Nginx

创建配置文件 `/etc/nginx/sites-available/capable`：

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    root /var/www/capable;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 启用 gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript;
}
```

#### 4. 启用配置

```bash
sudo ln -s /etc/nginx/sites-available/capable /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

---

## 📊 方案对比

| 方案 | 难度 | 费用 | 自动部署 | 自定义域名 | 推荐指数 |
|------|------|------|----------|------------|----------|
| Vercel | ⭐ | 免费 | ✅ | ✅ | ⭐⭐⭐⭐⭐ |
| Netlify | ⭐ | 免费 | ✅ | ✅ | ⭐⭐⭐⭐ |
| GitHub Pages | ⭐⭐ | 免费 | ✅ | ✅ | ⭐⭐⭐ |
| 自己的服务器 | ⭐⭐⭐⭐ | 付费 | ❌ | ✅ | ⭐⭐ |

---

## 🎯 推荐方案

### 新手用户
**推荐 Vercel**
- 最简单，点几下就完成
- 自动识别配置
- 性能最好

### 进阶用户
**推荐 Netlify**
- 功能更丰富
- 支持表单、函数等高级功能
- 同样简单易用

### 预算有限
**推荐 GitHub Pages**
- 完全免费
- GitHub 原生支持
- 适合个人项目

---

## 🔧 常见问题

### 1. 部署后页面空白？

**原因**: 路径配置问题

**解决方案**:
- Vercel/Netlify: 不需要配置 base 路径
- GitHub Pages: 需要在 `vite.config.ts` 中设置 `base: '/仓库名/'`

### 2. 刷新页面 404？

**原因**: SPA 路由问题

**解决方案**:
- Vercel: 已自动配置（通过 `vercel.json`）
- Netlify: 创建 `public/_redirects` 文件：
  ```
  /*    /index.html   200
  ```
- GitHub Pages: 创建 `public/404.html` 复制 `index.html` 内容

### 3. 构建失败？

**检查清单**:
```bash
# 1. 清理缓存
rm -rf node_modules package-lock.json
npm install

# 2. 本地测试构建
npm run build

# 3. 检查 Node.js 版本（推荐 18+）
node -v
```

### 4. 如何更新部署？

- **Vercel/Netlify**: 直接 `git push`，自动部署
- **GitHub Pages**: 运行 `npm run deploy`
- **自己的服务器**: 重新构建并上传

---

## 📱 PWA 支持

本项目已配置 PWA，用户可以：
- 添加到主屏幕
- 离线使用
- 获得类原生应用体验

部署后，用户在浏览器中访问时会看到"安装应用"提示。

---

## 🌐 分享给用户

部署完成后，你可以通过以下方式分享：

### 1. 直接分享链接
```
https://your-app.vercel.app
```

### 2. 生成二维码
使用在线工具（如 qrcode.com）生成二维码，方便手机扫码访问

### 3. 社交媒体分享
项目已配置 Open Graph 标签，分享到社交媒体会显示精美预览

---

## 🎉 部署成功后

恭喜！你的 Capa-ble 应用已经上线了！

### 下一步：

1. **测试功能**: 在不同设备上测试所有功能
2. **收集反馈**: 邀请朋友试用并提供反馈
3. **持续优化**: 根据用户反馈改进应用
4. **监控性能**: 使用 Vercel Analytics 或 Google Analytics

### 推广建议：

- 在社交媒体分享（微博、Twitter、小红书）
- 发布到产品社区（Product Hunt、V2EX）
- 写一篇介绍文章
- 制作使用教程视频

---

## 💡 技术支持

如果遇到问题：

1. 查看构建日志（Vercel/Netlify 控制台）
2. 检查浏览器控制台错误
3. 参考 [Vite 官方文档](https://vitejs.dev/)
4. 搜索相关错误信息

---

**祝你部署顺利！🚀**

如有问题，欢迎提 Issue 或联系开发者。
